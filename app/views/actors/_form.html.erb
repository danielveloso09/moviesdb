<%= form_with(model: actor, local: true) do |form| %>
  <% if actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actor.errors.count, "error") %> prohibited this actor from being saved:</h2>

      <ul>
      <% actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :actor_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :actor_last_name %>
  </div>

  <div class="field">
    <%= form.label :movie_title, "Movies:" %>
    <%= form.select( "movie", options_from_collection_for_select(@movies, "id", "title") ) %>
  </div>

  <div class="field">
    <%= form.label :birth_date %>
    <%= form.number_field :birth_date, id: :actor_birth_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
